import { ShareIntent } from "./types";

export const DEFAULT_INTENT: ShareIntent = {
  files: null,
  text: null,
  webUrl: null,
  type: null,
  conversationId: null,
};

export const LOG_TAG = "[useShareIntent]";
